# Server-side only. Do not prefix this variable with NEXT_PUBLIC_.
GITHUB_TOKEN=replace_with_read_only_github_token

# Optional comma-separated public repository fallback when no pinned repos are returned.
GITHUB_REPOSITORIES=wnba_tracker,daily_dose_backend,intramuralCS,Money-pool-solidity-nextjs,smile-visualizations,email_gpt3_extension,quiz_app
